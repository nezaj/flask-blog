{% extends "shared/layout.tmpl" %}

{% block title %}
  {{ post.title }} - Joe Averbukh
{% endblock %}

{% block content %}
  <div class="post-header">
    <h1><div class="post-title">{{ post.title }}</div></h1>
    <p>{{ post.timestamp.strftime('%B %d %Y') }}</p>
  </div>
  {{ content }}
{% endblock %}
